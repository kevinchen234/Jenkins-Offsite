minimal_for_validation:
  description: This is minimal for validation
  enabled: true

ext_one:
  description: IST is too expensive
  enabled: true

ext_two:
  description: IST is too slow
  enabled: true

ext_three:
  description: IST is too ugly
  enabled: true

